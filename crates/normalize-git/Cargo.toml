[package]
name = "normalize-git"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "DEPRECATED for in-workspace use, see normalize-vcs: pure-Rust read-only git operations (repo open, blob read, tree walk, diff, blame, churn, history)"

[dependencies]
anyhow = "1"
gix = { workspace = true }
