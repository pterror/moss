[package]
name = "normalize-vcs"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "VCS-agnostic trait boundary (domain types, not gix types) with a git backend; jj/hg backends are future work"

[dependencies]
normalize-git = { workspace = true }
gix = { workspace = true }
anyhow = "1"
